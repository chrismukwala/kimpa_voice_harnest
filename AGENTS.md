# AGENTS.md — Voice Harness

> Quick-start context for any AI coding agent working on this project.
> See `docs/` for deep dives on architecture, progress, and decisions.

## What is this project?

Voice Harness is a standalone desktop voice-driven coding assistant that replaces VS Code entirely. The user speaks a command; the system transcribes it (STT), sends it plus file context to a hosted Code LLM, receives SEARCH/REPLACE edit blocks + prose, shows edits as a diff for accept/reject, and reads the prose aloud (TTS). STT and TTS run locally; the LLM currently uses Gemini 2.5 Flash Lite via API.

## Tech stack

| Layer | Technology |
|---|---|
| UI framework | PyQt6 + PyQt6-WebEngine |
| Code editor | Monaco Editor 0.52.0 (AMD build) via QWebEngineView + QWebChannel |
| STT | Direct faster-whisper turbo + WebRTC VAD + sounddevice |
| Code LLM | Gemini 2.5 Flash Lite via OpenAI SDK (hosted, 100k char context) |
| TTS | Kokoro-82M (GPU when available, CPU fallback, Apache 2.0) |
| Edit format | Aider-style SEARCH/REPLACE blocks |
| Git | gitpython — auto-commit accepted changes |

## Dev environment

- **Python 3.11.x required** — validated audio stack target
- **CUDA toolkit** — nvcc must be on PATH
- **espeak-ng** — must be installed and on PATH (Kokoro dependency)
- **Windows 11** — primary target platform (Razer Blade 16, RTX 4080 12GB)

## Key commands

```bash
# First-time setup (creates venv, installs CUDA PyTorch first, then deps)
python setup/install.py

# Run the app
python main.py

# Run all tests (TDD — do this before every commit)
python -m pytest tests/ -v

# Manual audio diagnostics (full .venv only)
python tools/test_audio.py
python tools/test_mic.py

# Run Phase 0 Monaco POC (standalone test)
python phase0_poc/monaco_poc.py
```

## Development workflow — Red/Green/Refactor TDD

1. **Red**: Write a failing test in `tests/` that describes the new behaviour.
2. **Green**: Write the minimum production code to make it pass.
3. **Refactor**: Clean up while all tests stay green.
4. Run `python -m pytest tests/ -v` — all tests must pass before committing.

Every new feature or bug fix starts with a test. Code without a test is unfinished.

For Python changes, also follow `.github/instructions/python-correctness.instructions.md`: prefer
testable functional-core logic with explicit side-effect boundaries where that fits the codebase.

## Project layout

The module index below is auto-generated by `python scripts/lib/generate_docs.py`
from each file's module docstring. Do not edit between the markers by hand.

<!-- AUTO:modules -->

### harness/

| Module | Summary |
|---|---|
| `harness/audio_devices.py` | Audio device enumeration helpers. |
| `harness/audio_settings.py` | Persistent audio configuration storage. |
| `harness/code_llm.py` | Code LLM — OpenAI-compatible client + SEARCH/REPLACE parser. |
| `harness/coordinator.py` | Coordinator — queue pipeline: STT → context_assembler → LLM → response_splitter → TTS. |
| `harness/edit_applier.py` | Edit applier — apply SEARCH/REPLACE blocks to file content with security gates. |
| `harness/git_ops.py` | Git operations — auto-commit accepted changes. |
| `harness/llm_tools.py` | LLM tools — sandboxed file-system + test-running tools the LLM can call. |
| `harness/model_manager.py` | Model manager — STT/TTS model presence detection and lazy download. |
| `harness/repo_map.py` | Repo map — tree-sitter based repository symbol index for LLM context. |
| `harness/tts.py` | TTS — Kokoro text-to-speech adapter. |
| `harness/tts_navigator.py` | TTS Navigator — arrow-key sentence navigation + speed-adjusted playback. |
| `harness/voice_input.py` | Voice input — direct faster-whisper + WebRTC VAD. |

### phase0_poc/

| Module | Summary |
|---|---|
| `phase0_poc/monaco_poc.py` | Phase 0 — Monaco POC Gate |

### scripts/

| Module | Summary |
|---|---|
| `scripts/hooks/pre_commit.py` | Pre-commit hook entry point (Phase H1.1). |
| `scripts/hooks/pre_push.py` | Pre-push hook entry point (Phase H1.2). |
| `scripts/install_hooks.py` | Install Voice Harness git hooks (Phase H1). |
| `scripts/lib/_finding.py` | Shared types for ``scripts/lib/`` check modules. |
| `scripts/lib/check_file_sizes.py` | File-size limit check (Phase H1). |
| `scripts/lib/check_forbidden.py` | Forbidden-pattern check (Phase H1). |
| `scripts/lib/check_secrets.py` | Secret-scanning check (Phase H1). |
| `scripts/lib/generate_docs.py` | Auto-generated module index for AGENTS.md (Phase H3.1). |
| `scripts/lib/test_cache.py` | SHA-based test cache (Phase H1.3). |
| `scripts/lib/validate_docs.py` | Documentation drift detector (Phase H3.2). |

### setup/

| Module | Summary |
|---|---|
| `setup/install.py` | Voice Harness — Installation Wizard |

### tests/

| Module | Summary |
|---|---|
| `tests/conftest.py` | Shared fixtures for Voice Harness test suite. |
| `tests/test_ai_panel.py` | Tests for ui/ai_panel.py — AiPanel widget. |
| `tests/test_audio_devices.py` | Tests for harness/audio_devices.py. |
| `tests/test_audio_settings.py` | Tests for harness/audio_settings.py. |
| `tests/test_code_llm.py` | Tests for harness/code_llm.py — SEARCH/REPLACE parser + prose extractor. |
| `tests/test_code_llm_tools.py` | Tests for code_llm.chat_with_tools — multi-turn tool-calling loop. |
| `tests/test_coordinator.py` | Tests for harness/coordinator.py — message format, enqueue, lifecycle. |
| `tests/test_doc_generation.py` | Tests for doc-generation + drift-detector libraries (Phase H3). |
| `tests/test_edit_applier.py` | Tests for harness/edit_applier.py — apply SEARCH/REPLACE + security gates. |
| `tests/test_editor_panel.py` | Tests for ui/editor_panel.py — EditorPanel widget. |
| `tests/test_git_ops.py` | Tests for harness/git_ops.py — auto-commit accepted changes. |
| `tests/test_hooks.py` | Tests for git-hook check libraries (Phase H1). |
| `tests/test_instructions_files.py` | Structural tests for `.github/instructions/*.instructions.md` (Phase H2). |
| `tests/test_llm_tools.py` | Tests for harness/llm_tools.py — sandboxed tool dispatcher for the LLM. |
| `tests/test_main_window.py` | Tests for ui/main_window.py — startup wiring + editor-coordinator sync. |
| `tests/test_model_manager.py` | Tests for harness.model_manager — STT/TTS model presence + lazy download. |
| `tests/test_repo_hygiene.py` | Repository hygiene gates (Phase H4). |
| `tests/test_repo_map.py` | Tests for harness/repo_map.py — tree-sitter repo map generation. |
| `tests/test_tts.py` | Tests for harness/tts.py — sentence splitting + speak/play contracts. |
| `tests/test_tts_navigator.py` | Tests for harness/tts_navigator.py — TTS sentence navigation + playback. |
| `tests/test_voice_input.py` | Tests for harness/voice_input.py — VoiceInput with direct faster-whisper + WebRTC VAD. |

### tools/

| Module | Summary |
|---|---|
| `tools/test_audio.py` | Manual audio smoke test for TTS playback diagnostics. |
| `tools/test_mic.py` | Manual microphone smoke test for Voice Harness. |

### ui/

| Module | Summary |
|---|---|
| `ui/ai_panel.py` | AI panel — response display + voice status + manual input + TTS controls. |
| `ui/editor_panel.py` | Editor panel — Monaco Editor via QWebEngineView + QWebChannel (Phase 2b). |
| `ui/main_window.py` | Main window — 3-panel layout: file tree \| editor \| AI panel. |

<!-- /AUTO:modules -->

## Critical constraints — read before making changes

1. **PyTorch CUDA must install FIRST** — before faster-whisper/Kokoro or any other dep, or CPU-only torch gets locked in.
2. **ctranslate2 pinned to 4.4.0** — ≥4.5.0 requires cuDNN 9.2 which conflicts with this GPU setup.
3. **`--in-process-gpu` Chromium flag** — required for QWebEngineView on this dual-GPU laptop (Intel iGPU + RTX 4080 Optimus). Set via `QTWEBENGINE_CHROMIUM_FLAGS` before any Qt imports.
4. **`QTWEBENGINE_DISABLE_SANDBOX=1`** — required on Windows for QtWebEngine.
5. **Monaco must be served via localhost HTTP** — NOT `file://` (Web Worker restrictions) or custom `app://` scheme (QBuffer GC issues). This is proven in Phase 0 POC.
6. **`voice_input.py` owns voice capture/transcription** — keep sounddevice/WebRTC VAD/faster-whisper details behind this module boundary.
7. **Coordinator message format**: `{"query": str, "context": str|None, "repo_map": str|None}` — never plain strings.
8. **`tts.py` returns `List[Tuple[str, bytes]]`** — sentence-split WAV chunks, required for Phase 4 arrow-key navigation.
9. **VRAM budget is 12GB** — faster-whisper must use `compute_type="int8_float16"` (NOT fp16). LLM runs in the cloud (Gemini 2.5 Flash Lite) — no local VRAM needed.
10. **Apache 2.0 license** — see LICENSE file. Note: PyQt6 itself is GPL v3.

## After completing work

- Run `python -m pytest tests/ -v` — all tests must pass.
- Update `docs/PROGRESS.md` with what you changed and the new phase status.
- Keep `.github/instructions/python-correctness.instructions.md` current when Python correctness,
  testing, or design guidance changes.
- Commit with a conventional commit message: `feat:`, `fix:`, `refactor:`, `docs:`, `test:`.
- Do NOT run `git add .` or `git add -A` — stage specific files only. <!-- pragma: allow forbidden -->

## Docs map

| Topic | File |
|-------|------|
| Architecture | [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) |
| Conventions | [docs/CONVENTIONS.md](docs/CONVENTIONS.md) |
| Decisions | [docs/DECISIONS.md](docs/DECISIONS.md) |
| Setup | [docs/SETUP.md](docs/SETUP.md) |
| Progress | [docs/PROGRESS.md](docs/PROGRESS.md) |
| Audio UX plan | [docs/PLAN_AUDIO_UX.md](docs/PLAN_AUDIO_UX.md) |
| Harness Engineering plan | [docs/PLAN_HARNESS_ENGINEERING.md](docs/PLAN_HARNESS_ENGINEERING.md) |
| Readiness rubric | [docs/READINESS.md](docs/READINESS.md) |
| Review report | [docs/REVIEW_REPORT.md](docs/REVIEW_REPORT.md) |
| Red Team report | [docs/RED_TEAM_REPORT.md](docs/RED_TEAM_REPORT.md) |
